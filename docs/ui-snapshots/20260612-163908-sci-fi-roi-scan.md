# ROI Scan Snapshot

Timestamp: `2026-06-12 16:39:08`

Purpose: lưu lại bản scan ROI hiện tại sau khi tham chiếu từ `C:\Users\AH\Downloads\sci_fi_scanner.html`.

## Files

- `frontend/components/operator/operator-roi-editor.tsx`
- `frontend/app/globals.css`

## ROI JSX Snapshot

```tsx
<div className="pointer-events-none absolute inset-0 overflow-hidden">
  <div className="operator-roi-scan-surface" />
  <div className="operator-roi-scan-frame">
    <span className="operator-roi-scan-corner corner-tl" />
    <span className="operator-roi-scan-corner corner-tr" />
    <span className="operator-roi-scan-corner corner-bl" />
    <span className="operator-roi-scan-corner corner-br" />
  </div>
  <div className="operator-roi-scan-grid-container">
    {Array.from({ length: ROI_SCAN_GRID_SIZE * ROI_SCAN_GRID_SIZE }).map(
      (_, cellIndex) => {
        const row = Math.floor(cellIndex / ROI_SCAN_GRID_SIZE);
        const column = cellIndex % ROI_SCAN_GRID_SIZE;

        return (
          <span
            key={`${region.index}-${cellIndex}`}
            className="operator-roi-scan-cell"
            style={{
              animationDelay: `${region.index * 180 + row * 220 + column * 24}ms`,
            }}
          />
        );
      },
    )}
  </div>
  <div
    className="operator-roi-scan-glow"
    style={{ animationDelay: `${region.index * 180}ms` }}
  />
  <div
    className="operator-roi-scan-line"
    style={{ animationDelay: `${region.index * 180}ms` }}
  />
</div>
```

## CSS Snapshot

```css
@keyframes operator-roi-scan-line-move {
  0% {
    top: 0;
    opacity: 1;
  }
  92% {
    top: calc(100% - 2px);
    opacity: 1;
  }
  96% {
    top: calc(100% - 2px);
    opacity: 0;
  }
  100% {
    top: 0;
    opacity: 0;
  }
}

@keyframes operator-roi-scan-glow-move {
  0% {
    top: 0;
    opacity: 1;
  }
  92% {
    top: calc(100% - 56px);
    opacity: 1;
  }
  96% {
    top: calc(100% - 56px);
    opacity: 0;
  }
  100% {
    top: 0;
    opacity: 0;
  }
}

@keyframes operator-roi-cell-flash {
  0% {
    background: rgba(255, 220, 0, 0.95);
    border-color: rgba(255, 221, 87, 0.92);
    box-shadow: inset 0 0 10px rgba(255, 248, 180, 0.9);
    opacity: 1;
  }
  35% {
    background: rgba(255, 200, 0, 0.6);
    border-color: rgba(255, 210, 77, 0.7);
  }
  70% {
    background: rgba(255, 190, 0, 0.22);
    border-color: rgba(255, 214, 10, 0.26);
    box-shadow: none;
  }
  100% {
    background: transparent;
    border-color: rgba(255, 214, 10, 0.12);
    box-shadow: none;
    opacity: 0.45;
  }
}

.operator-roi-scan-surface {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 50% 50%, rgba(0, 255, 231, 0.08), transparent 70%),
    rgba(0, 255, 231, 0.04);
}

.operator-roi-scan-frame {
  position: absolute;
  inset: 0;
}

.operator-roi-scan-corner {
  position: absolute;
  width: 10px;
  height: 10px;
  border-color: rgba(0, 255, 231, 0.95);
  border-style: solid;
  opacity: 0.9;
  filter: drop-shadow(0 0 6px rgba(0, 255, 231, 0.45));
}

.operator-roi-scan-corner.corner-tl {
  top: -1px;
  left: -1px;
  border-width: 2px 0 0 2px;
}

.operator-roi-scan-corner.corner-tr {
  top: -1px;
  right: -1px;
  border-width: 2px 2px 0 0;
}

.operator-roi-scan-corner.corner-bl {
  bottom: -1px;
  left: -1px;
  border-width: 0 0 2px 2px;
}

.operator-roi-scan-corner.corner-br {
  bottom: -1px;
  right: -1px;
  border-width: 0 2px 2px 0;
}

.operator-roi-scan-grid-container {
  position: absolute;
  inset: 0;
  display: grid;
  grid-template-columns: repeat(8, 1fr);
  grid-template-rows: repeat(8, 1fr);
  gap: 1px;
  padding: 4px;
}

.operator-roi-scan-cell {
  border: 1px solid rgba(0, 255, 231, 0.06);
  background: transparent;
  opacity: 0.4;
  animation: operator-roi-cell-flash 2.6s linear infinite;
}

.operator-roi-scan-glow {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  height: 56px;
  background: linear-gradient(to bottom, rgba(0, 255, 231, 0.12), transparent);
  animation: operator-roi-scan-glow-move 2.6s linear infinite;
}

.operator-roi-scan-line {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  height: 2px;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(0, 255, 231, 0.1),
    rgba(0, 255, 231, 1),
    rgba(0, 255, 231, 0.1),
    transparent
  );
  box-shadow:
    0 0 12px 3px rgba(0, 255, 231, 0.4),
    0 0 2px rgba(255, 255, 255, 0.65);
  z-index: 2;
  animation: operator-roi-scan-line-move 2.6s linear infinite;
}
```

## Notes

- Snapshot này chỉ lưu phần scan effect, không lưu toàn bộ file.
- Muốn restore, copy lại các block trên vào đúng vị trí hiện tại.
