fastapi
uvicorn[standard]
pydantic
numpy
opencv-contrib-python
pypylon
websockets

# AI / yolo_ocr (kéo theo torch). Để dùng GPU (CUDA), cài torch bản CUDA tương ứng
# theo hướng dẫn pytorch.org trước; mặc định ultralytics cài torch CPU.
ultralytics
